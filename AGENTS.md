# AGENTS.md

## 执行规范

- **范围约束**：默认严格按用户要求的字面范围执行。除非用户明确要求，不得新增文档、脚本、备份、框架、重构、影响分析、扩展性设计或额外验证。
- **错误闭环**：默认按“执行一次 → 查看首个真实错误 → 最小修复 → 从失败点继续或重跑”闭环处理。真实错误出现前，不得堆叠预防性检查或推测性修复。
- **检查复用**：同一任务指纹（目标、对象、会话、环境及关键前提相同）下，已经通过的路由、Skill、恢复、连接和环境检查不得重复执行。仅在对应前提实际变化时重新检查。
- **停止条件**：达到用户要求的最小验收条件后必须立即停止。发现相邻问题时最多用一句话提示，不得自动扩大范围处理。

## 项目约束

- 从 `docs/README.md` 开始，按当前阶段实现，不提前建设后续阶段。
- 依赖只能向内：`入口/具体实现 -> 用例包 -> live`；`live` 只依赖 Go 标准库。
- 按业务能力组织包；核心规则放在 `live` 或拥有该规则的用例包，外部细节只留在 server、store 和具体供应商/媒体实现包。
- 接口由使用方定义；没有替换或测试需求时，不为每个类型创建接口。
- 保持实现直接、错误有上下文、重试有上限，不写猜测性的防御代码。
- 只执行与本次改动直接相关的最小格式化和验收；不得借机修改无关代码或文档。

## 后端质量反馈

- 后端完整入口为 `make go-verify`；环境与各阶段见根 README。使用 Makefile 固定的工具与 Go toolchain，CI 复用同一套规则。
- 先执行与修改直接相关的检查，失败后从该阶段继续。不得通过文件特例、nolint、降低阈值、忽略业务源码或跳过集成测试获得通过。
- Go 文件统一不超过 500 个非空、非注释行。按真实职责拆分；测试函数只免除函数长度和认知复杂度检查，不免除文件大小及正确性检查。
- `make test` 不等于完整验收。集成测试使用可丢弃的 PostgreSQL 数据库与真实 FFmpeg；缺少环境应修复环境，不改成 skip。
- 完成后报告实际执行的阶段、结果及未完成项；本地成功不得描述为远端 CI 成功，未经请求不提交或推送。
